import { Button } from "@mui/material";

const Buttons = () => {
    return (
      <>
        <Button variant="outlined"
          onClick={() => {
            alert('clicked');
          }}>
          9mm</Button>
      </>
    );
  };
  
  
  
  
  export default Buttons
  